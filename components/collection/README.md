# Collection

Renders the collection SVG icon used in search result rows.

## Usage

```twig
{% include 'metsis_drupal:collection' with {
  icon_alt_text: 'Collection',
  width: 120,
  height: 42
} only %}
```
