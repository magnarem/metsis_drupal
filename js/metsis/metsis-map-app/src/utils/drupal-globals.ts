/* May not be needed after alll. consider removing */
/*const { Drupal, drupalSettings } = window as any;
const { jQuery } = window as any;
export const getDrupal = () => Drupal;
export const getJQuery = () => jQuery;
export const useDrupalSettings = () => drupalSettings;
export const getMapAppSettings = () => drupalSettings?.metsisMapApp;
*/
